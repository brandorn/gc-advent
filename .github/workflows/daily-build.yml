name: Daily build

on:
  schedule:
    - cron:  '0 * * * *' # Update to '0 0 * * *'

jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
    - name: curl
      uses: wei/curl@master
      with:
        args: "-X POST -H 'Authorization: token ${{ secrets.aafa345c4da2d4159d8565c5fff1be9ad2da3d0c }}' -H 'Accept: application/vnd.github.ant-man-preview+json' https://api.github.com/repos/brandorn/brandon.github.io/gc-advent/pages/builds"
